# DataImprove DFS handoff for Claude Code / Fable 5

## Read order

1. Read `AGENTS.md`; it is the workspace-wide authority.
2. Read `CLAUDE.md`; its `Active handoff` section points here through
   `handoff/current.yaml`.
3. Parse `STATE.yaml`; it is the sole authority for facts, task status, roles,
   and checkpoints.
4. Read only the documents named by the active task's `inputs` in `STATE.yaml`.

## Goal

Resume the approved DataImprove depth-first search at R25. Design a procedural
multiscale tissue microtexture sibling from the clean R19 base without inheriting
the R23 vessel specialization or the R24 real-exemplar specialization. The
current handoff stops before code, branch creation, rendering, gap comparison,
or training; see tasks `T-001` through `T-005`.

## Current position

- The repository and worktree topology is captured by `F-GIT-001` through
  `F-GIT-009`.
- The appearance DFS evidence boundary is summarized by `F-EXP-003` through
  `F-EXP-011` and the invalid-source checkpoint `C-F008`.
- R23 and R24 are siblings from R19 rather than a cumulative stack; see
  `F-GIT-008`.
- R24's authoritative Chinese recovery and the preserved legacy English report
  are bound by documents `D-004` through `D-006`.
- The next authorized work is the read-only preflight `T-001`.

## Scope delta

This handoff narrows `AGENTS.md`; it does not override it.

- Preserve the three user-owned untracked root files recorded by
  `repository.preexisting_untracked`; do not open, edit, move, delete, stage, or commit them
  unless the user separately authorizes that action.
- Keep every failed or superseded verification artifact no-clobber; see
  `C-F010`.
- Treat `seq04` as permanently invalid under `F-EXP-002`.
- Enforce the resource gate in `C-F011` and the experiment gates in tasks
  `T-002` through `T-007`.
- Git writes remain human-gated. The only prospective worktree action appears
  after `T-004` and is defined by `T-005`.

## Fable 5 compliant execution

The work is non-clinical computer graphics, synthetic-data engineering, and
domain-gap research. Its safety and data-governance boundary is authoritative in
`fable5_policy`.
"Vessel texture" means only a pixel or surface-appearance field.

Use Claude Code at the minimum version and model recorded in `fable5_policy`.
Safety refusal or routing is a valid checkpoint, never something to evade. When
one occurs, follow `C-H003` and task-level scope reduction rather than
rewriting the request deceptively.

Current host preflight: `claude.cmd --version` reports Claude Code `2.1.131`,
which is below the required `2.1.170`; therefore `T-001` is blocked until an
authorized upgrade and active-model confirmation. The PowerShell `claude.ps1`
launcher is blocked by the host execution policy, while the `.cmd` launcher
works. Do not change the host execution policy merely to bypass that launcher
failure.

Official references:

- [Claude Fable 5](https://www.anthropic.com/claude/fable)
- [Claude Fable 5 and Claude Mythos 5 announcement](https://www.anthropic.com/news/claude-fable-5-mythos-5)
- [Claude Code releases](https://github.com/anthropics/claude-code/releases)
- [Claude Code data usage](https://code.claude.com/docs/en/data-usage)
- [Anthropic commercial retention](https://privacy.anthropic.com/en/articles/7996866-how-long-do-you-store-my-organization-s-data)
- [Anthropic zero-data-retention scope](https://privacy.anthropic.com/en/articles/8956058-i-have-a-zero-data-retention-agreement-with-anthropic-what-products-does-it-apply-to)

## Git map

Use the exact commits and ancestry in `F-GIT-001` through `F-GIT-008`. Do not
infer a cumulative R19-to-R24 chain. Before any future
worktree creation, `T-004` must be complete and `T-005` must revalidate the
prospective base and same-drive sibling location.

## What has been proved

The accepted or limited outcomes are facts `F-EXP-003` through `F-EXP-011`. In
particular, only `F-EXP-009` records an unambiguously positive visual result,
while its claim boundary remains restricted to its registered views.
`F-EXP-011` proves
registered-view continuity and a bounded source-boundary observation, while its
spatial diversity remains limited. Neither supports an appearance-gap or
training-benefit claim.

## Failure checkpoints

- `C-F002` separates the initial environment failure from the later native
  Windows technical result.
- `C-F008` preserves the invalid R24 v1 source gate.
- `C-F010` preserves orchestration incidents, including the legacy English report
  and its
  no-clobber Chinese recovery.
- `F-GIT-009` prevents a historical dirty-worktree classification from being
  presented as current.
- `C-H003` defines refusal capture and safe fallback.
- `C-H001`, `C-H004`, and `C-H005` are the in-file verification checkpoints for
  this handoff.

## Workflow

Execute `T-001` through `T-009` strictly by dependency and status. Stop at the
first failed acceptance condition. `T-005` is the first task that may create a
worktree or write implementation code, and only after the human approval gate
`T-004`. `T-008` is a separate human commit gate; design/worktree approval is
not commit approval. Training and gap comparison remain forbidden until the prospective R25
preview, resource, visual, and closure gates have passed.

## Agent assignments

Canonical role meanings come only from `AGENTS.md#collaboration-protocol`.
Current assignments and task ownership are machine-readable under `roles` and
`tasks` in `STATE.yaml`. Fable coordinates design and final review; mechanical
search, tests, hashes, and receipts should be delegated when permitted, with the
same safety and data boundary.

## Immediate next action

Resolve the version blocker in `C-H002`, then rerun `T-001` only: read-only
preflight and fact confirmation. If it passes, move to `T-002`; do not create
R25 code or a worktree during either task.

## Claim boundary

The supported and unsupported claims are listed once under `claim_boundary` in
`STATE.yaml`. This handoff is a coordination and provenance record, not new
scientific evidence. It cannot promote a limited or inconclusive result, infer a
training benefit, or claim that the appearance gap has closed.

## Verification artifact roster

| Path | Purpose | Owner | Cleanup |
|---|---|---|---|
| `CLAUDE.md` | Stable Claude Code auto-entry | Improve Agent | Preserve |
| `handoff/current.yaml` | Active handoff pointer | Improve Agent | Preserve |
| `handoff/2026-08-09_data-improve/HANDOFF.md` | Human-readable handoff | Improve Agent | Preserve |
| `handoff/2026-08-09_data-improve/STATE.yaml` | Sole machine-readable state | Improve Agent | Preserve |

The later verification result is recorded in-place as `C-H001`, `C-H004`, and
`C-H005`; no additional handoff framework or scratch artifact is authorized.
