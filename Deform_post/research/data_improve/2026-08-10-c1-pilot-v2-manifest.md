# 实验配置清单 C1-PILOT-V2-20260810（texel 烘焙纹理试点，跑前交检）

## 1. 目的与预注册判据

- **目的**：C1 设计 v2/v2.1（texel 烘焙真纹理 + 真实语料色彩锚定）在 seq01 上产出 5 个种子变体，交所有者目检裁定"纹理真实可见、颜色贴近真实分布、论文可解释"。**不做任何 Gap 主张。**
- **预注册判据**：
  1. 机械：每种子 1716 帧全渲出、F2 错误日志空、F1 零空白帧、序列化对账通过；烘焙贴图 sha256 入溯源；
  2. 色彩统计门：渲染器官区均值落真实盒 [p05×0.9, p95×1.1] 带内（冒烟已过，试点复核）；
  3. 纹理可见性门（v2.1 口径=**逐帧最小值** ≥ 0.02）：由振幅下限 0.13 结构性保证，试点抽签全在 0.137–0.160；
  4. 终门：所有者目检（蒙太奇含白模基线行与**真实帧参照行**）。

## 2. 与 v1 试点的差异（回应否决理由）

| 否决点 | v2 对策 |
|---|---|
| 无纹理贴图 | R25 连续场经 xatlas UV 烘焙成 1024² 真贴图（覆盖率 0.635，约 66 万有效 texel，较顶点色 2005 采样提升两个数量级），R16 已验证贴图渲染路径 |
| 颜色游戏感/脱离现实 | 色域改为真实语料经验支撑集（31 序列 310 帧统计，p05–p95 盒），器官区渲染均值经两轮标定乘子 [1.0656, 1.0068, 1.0404] 校准落带；uniform 平涂模式删除 |
| 可解释性 | 论文表述 = randomization within the empirical support of the real corpus；蒙太奇直接并排真实帧 |

## 3. 数据与代码溯源

- 输入同 v1 清单（seq01 生产 PLY/相机/标签，只读）；输出 `_c1_scratch/pilot_v2/seed<k>/`。
- 代码：branch@3a8ac0e + v2 工作集（未提交，目检通过后随收尾提交）；UV sidecar `kidney-anat-xatlas-v1.npz`（sha256 8dee381b…，其 receipt 记录的 canonical 哈希 = 生产网格 f0a301cf… 逐字符相符）；R25 场模块 = 9c74cf0 逐字节提取件。
- 实现验证（跑前已完成）：TDD 53 红→全套件 105 绿；OFF 态三层字节等同（两形态 × 前 50 帧）；冒烟色彩门过、后处理确定性过；v2.1 修订（振幅下限 0.10→0.13 + 可见性门口径钉死逐帧最小值）已入设计文档并复跑全套件 105 绿。

## 4. 种子计划（预抽签在案）

注册种子 1–4：c3/a=0.1385、c3/a=0.1399、c1/a=0.1368、c1/a=0.1441；按预声明规则（>4 最小抽中缺失变体者）补种子 10：c2/a=0.1603——三变体全覆盖。蒙太奇 7 行 = 白模基线 + 5 种子 + 真实参照（real_full/seq01，按序位取帧）。

## 5. 命令（逐字，k ∈ {1,2,3,4,10}）

同 v1 清单格式，工作目录 `Deform_post`，config 换 `_c1_scratch/config_pilot_v2.yaml`，输出根 `_c1_scratch/pilot_v2/`；蒙太奇命令含 `--baseline` 与 `--real-dir "..\DataFlow\Deform_post\primary\real_full\seq01\png"`。

## 6. 环境与成本

同 v1（本机 MedLearning env）；新增烘焙开销每种子 0.3–7.4s（变体而异）；预计每种子约 5 分钟，总计约 30 分钟；磁盘约 10G 落 scratch。

## 7. 术语表（新增项；v1 清单术语表继续有效）

| 术语 | 解释 | 来路 |
|---|---|---|
| texel 烘焙 | 把三维连续场逐纹理像素采样、预计算成贴图文件 | `dpost/texture_bake.py`（本工作集）；机制条目 R25-LIT-08 solid texturing |
| xatlas / UV 参数化 | 把三维表面展开到二维贴图坐标的工具/过程 | xatlas 0.0.11（MedLearning env）；sidecar 出自 R16A formal-v2 |
| gutter 膨胀 | 贴图 chart 边界外扩几像素防双线性采样渗色 | `texture_bake.py` gutter_px=4；图形学通用工艺 |
| 经验支撑集 | 真实数据实际出现的取值范围（此处 p05–p95 盒） | `_c1_scratch/real_color_anchor.json` 推导注记 |
| 标定乘子 | 补偿渲染光照使器官区均值落回真实带的全局逐通道乘数 | 设计 v2-2；冒烟两轮实测 [1.0656,1.0068,1.0404] |
| 覆盖率 | UV 面在贴图上实际占用的 texel 比例 | `texture_bake.py` 输出（实测 0.6348） |
| 逐帧最小值口径 | 可见性门对每帧分别计算、取最小值判定，杜绝均值掩盖弱帧 | 设计 v2.1 修订段 |
| 循环旋转面片 | DeformSim 帧 PLY 的面行相对 canonical 逐行旋转但保绕向 | R16 `validate_oriented_face_rows`；T-006 探针诊断先例 |

## 8. 运行回执（2026-08-10 协调者填写）

- 5 种子全部 1716/1716 帧渲出、F2 错误日志为空、序列化各 1716 样本；每种子约 10.2 分钟（纹理网格逐帧重建较 v1 慢一倍，属预期）。贴图 sha256 与全部抽样值入各 `appearance_meta.json`（例：seed4 贴图 63021185…、seed10 966f7be8…）。
- 色彩统计门试点复核：15 帧抽检（5 种子 × 3-4 帧）**13 帧落带**；2 帧 R 通道边际超上界 0.0005–0.003（≤0.3%，亮帧后处理乘子叠加所致）——如实记录，是否要求后处理链加带内钳制由所有者裁定。
- 纹理可见性：v2.1 振幅下限生效，试点抽签 a∈[0.137,0.160] 全部高于冒烟实测的过门线 0.122。
- 蒙太奇 7 行（基线 + 5 种子 + 真实参照）已生成并交目检。
- 本回执不构成任何 Gap/训练主张；全程无 git 操作、无生产数据写入。
