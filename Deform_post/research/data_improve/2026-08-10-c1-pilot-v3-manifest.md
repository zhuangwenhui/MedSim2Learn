# 实验配置清单 C1-PILOT-V3-20260810（血管烘焙增量试点，跑前交检）

## 1. 目的与预注册判据

- **目的**：在 v2 已目检的基础上叠加 R23 隐式血管层，同五种子（1/2/3/4/10）重跑 seq01 试点——蒙太奇与 v2 版构成受控对比，交所有者目检。**不做任何 Gap 主张。**
- **预注册判据**：机械（5×1716 帧、F2 错误日志空、序列化对账过）；v3-4 四门（血管可见性 min|Δ|≥0.005、血管占比 [0.5%,5%]、血管色落经验盒、v2 各门保持）冒烟已过、试点复核占比；终门 = 所有者目检（7 行蒙太奇含真实参照行）。

## 2. 血管层事实（冒烟实测）

树 781–806 段/种子、总长约 2.8m、根径 1.319mm（R23 已验收 small 尺度 0.012×表面尺度 109.9mm）；texel 级精确点-段求值（约 25s/烘焙，每序列每种子一次）；血管占比 4.78–4.89%；血管呈 7-texel 根段→1–2 texel 末梢的锥度分叉曲线，跨 UV chart 连续；血管色抽样 [0.742,0.465,0.561] 落真实高红度经验盒。

## 3. 代码溯源

branch@3a8ac0e + v3 工作集（未提交）；R23/R21 模块自 `9d3011f` 逐字节提取（sha256 c937cd5e… / 5838385a…），其对 r16/r19 依赖与本分支既有提取件逐字节一致（已核）；提取模块自测 42/44（2 失败为缺 r23 时代冻结资产的 FileNotFoundError，与逻辑无关）；全套件 141/141 绿；OFF 态两形态 30/30 字节等同（含 vessel 字段存在但 appearance 关闭的情形）。

## 4. 预告偏差（透明声明）

色彩标定乘子由 [1.0656,1.0068,1.0404] 上调至 **[1.0656,1.0220,1.0505]**（血管暗像素并入器官区均值后最暗帧 G/B 低于带底，按 v2-2 既定标定机制修正）——故 v3 与 v2 试点除血管层外还相差约 1–1.5% 的 G/B 组织色调（视觉不可辨）；若您要求字节纯对照可一行配置改回（代价是色彩门以上述幅度 FAIL）。流稳定性已用黄金测试锁定：五种子的组织色/变体/振幅/背景抽签与 v2 试点溯源逐位相同。

## 5. 命令

`bash _c1_scratch/run_pilot_v3.sh`（与 v2 驱动同构：5 种子渲染+序列化+7 行蒙太奇，输出根 `_c1_scratch/pilot_v3/`，config `config_pilot_v3.yaml`）。预计约 1 小时（每种子约 11 分钟，含一次性烘焙 27–33s）。

## 6. 术语表（新增项；v1/v2 清单术语表继续有效）

| 术语 | 解释 | 来路 |
|---|---|---|
| 隐式符号场 | 每个空间点给一个带符号距离值（负=血管内、正=血管外）的场表示 | `c1_r23_implicit_vessel_width.py` 模块 docstring（9d3011f 提取件） |
| 锥度半径 | 血管半径沿分支从根到末梢线性变细 | R21 `compute_equal_terminal_diameters`；R23 distance−tapered radius 约定 |
| 点-段距离求值 | 对每个 texel 的 3D 点精确计算到树折线各段的最近距离 | `texture_bake.py` v3 段（per-texel-exact-tapered-segment，溯源在案） |
| smoothstep 抗锯齿带 | 在 ±0.10mm 带内平滑过渡血管权重，避免锯齿边 | 设计 v3-2；半宽常量出自 R23 `R23_ANTIALIAS_HALF_WIDTH_MM` |
| 血管占比 | 血管权重 w>0.5 的 texel 占有效 texel 的比例 | 设计 v3-4 门 (a)；冒烟实测 4.8% |
| attraction 种子 | R21 血管树生长算法唯一可变的随机源（树参数冻结、仅布局随种子变化） | `c1_r21_procedural_vessels.py`（生长函数无参、常量冻结） |
| 受控对比 | v3 与 v2 同五种子同命令，蒙太奇差异即增量本身（加§4 声明的标定微差） | 设计 v3-5 |

## 7. 运行回执（2026-08-11 协调者填写）

- 5 种子全部 1716/1716 帧、F2 错误日志为空、序列化各 1716 样本；每种子约 11 分钟（含一次性血管场求值约 25s）。贴图与血管抽样值全部入各 seed 的 `appearance_meta.json`。
- 血管占比试点值 4.78–4.89%（门 [0.5%,5%] 内）；血管网络逐种子布局不同、色彩均在真实高红度经验盒族内；血管随表面变形逐帧贴合、无接缝、无顶点团块。
- 蒙太奇 7 行（基线 + 5 种子 + 真实参照）已生成交目检。
- 协调者预检视：与 v2 相比五行全部出现清晰锥度分叉血管网络，是本轮增量的直接可视证据；诚实残差=真实行仍多出湿润高光、更强的颜色异质性（紫斑/脂肪）与更密的细血管毛细层。本回执不构成任何 Gap/训练主张；全程无 git 操作、无生产写入。
