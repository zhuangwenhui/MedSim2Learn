# C1 外观域随机化设计（Track C / H1a，试点先行）

> **v3 修订（2026-08-10，所有者选 B：血管烘焙增量；在 v2/v2.1 之上叠加，其余条款不变）**
>
> **v3-1 血管层来源。** R23 隐式血管场资产按文件自 r23 分支提交 `9d3011f` 逐字节提取
> （`c1_r23_implicit_vessel_width.py` + import 闭包 `c1_r21_procedural_vessels.py`；
> 提取前必须核对其对 r16/r19 模块的依赖版本与本分支已提取件是否一致，不一致须报告
> 处置）。尺度冻结为 R23 已验收的 small（surface_ratio 0.012），抗锯齿半宽 0.10mm
> 沿用；R23 的 controlled-diffuse 光照常量**不**流入（C-F007，本管线光照冻结于生产值）。
> **v3-2 合成方式。** 在 texel 烘焙阶段对每个 texel 的 3D 点求值血管符号场 s，
> 权重 w = 抗锯齿带上的 smoothstep（s≤−h→1，s≥+h→0），
> texel 色 = mix(组织纹理色, 血管色, w)。血管树按序列种子生成（采样器第 4 子流，
> root.spawn(4)[3]，**前三子流抽签不变**——流稳定性必须以测试锁定）；树参数沿
> R21/R23 冻结值，仅种子变化布局。
> **v3-3 血管色锚定。** 血管色逐序列在真实高红度经验盒内抽取：p05–p95 =
> R[0.661,0.798] G[0.419,0.559] B[0.469,0.622]（推导 `_c1_scratch/real_vessel_anchor.json`，
> 310 帧红度 top-3% 像素统计）；同受色彩标定乘子作用；抽样值入溯源。
> **v3-4 新增机械门。** (a) 血管可见性：带血管烘焙 vs 同参数无血管烘焙的渲染器官区
> |Δ| 逐帧最小值 ≥ 0.005 且血管 texel 占比落 [0.5%, 5%]（对应 small 尺度的合理投影）；
> (b) 血管色带内：血管权重 w>0.5 的 texel 均值落血管盒；(c) 既有 v2 各门全部保持；
> (d) OFF 态字节等同不变。
> **v3-5 试点对照设计。** 复用 v2 的同五种子（1/2/3/4/10）——蒙太奇与 v2 版唯一差异
> 即血管层，构成受控对比；7 行同构（基线 + 5 种子 + 真实参照）。终门 = 所有者目检。

> **v2 修订（2026-08-10，目检门否决后重设计；本节覆盖 v1 §2/§4 的旋钮表与试点方案，其余节仍有效）**
>
> **否决理由记录（所有者裁定要义）**：v1 产物"单纯改颜色、无纹理贴图"，且部分颜色
> 过分脱离现实（游戏贴图感），即使缩小 Gap 论文可解释性也极差。验证成本意识本身无错。
>
> **v2-1 纹理载体：texel 级烘焙（替代顶点色）。** canonical 网格 xatlas UV 参数化
> （优先复用 R16 sidecar `kidney-anat-xatlas-v1.npz`，哈希核验可用则用、否则以固定
> 参数重新生成并记录）；对 1024² 贴图上每个被 UV 面覆盖的 texel 反映射到表面 3D 点
> （重心坐标），经 R25 归一化求值连续场 → 真纹理贴图；gutter 膨胀 4px 防双线性渗色。
> 每序列每种子烘焙一次，全部变形帧同拓扑复用同一 UV+贴图（纹理跟随表面）。渲染走
> R16 已验证的 `triangle_uvs + textures` 路径。**不重蹈 R17**：被采样的场在三维物体
> 空间连续，chart 边界两侧场值由构造近同，接缝不存在于值域，只需 gutter 防滤波渗色。
>
> **v2-2 颜色真实性锚定（论文可解释性硬约束）。** 随机化色域不再手拍，改为真实语料
> 经验支撑集：31 序列 × 10 帧 = 310 帧的 FOV 内像素统计（推导与数值记录于
> `_c1_scratch/real_color_anchor.json`）——器官基色盒 = 亮区（lum≥q60）逐帧均值的
> p05–p95：R[0.818,0.860] G[0.694,0.752] B[0.742,0.787]；背景盒 = 暗区（lum≤q35）
> p05–p95：R[0.394,0.458] G[0.283,0.336] B[0.330,0.387]。论文表述：randomization
> within the empirical support of the real corpus。渲染受光照影响，冒烟测试须验证
> "渲染后器官区均值落在真实盒 [p05×0.9, p95×1.1] 带内"，允许一个全局标定乘子，
> 乘子值写入溯源。
>
> **v2-3 模式与调制。** uniform 平涂模式**删除**（0%）——所有变体必须带纹理；三个
> R25 变体等概率；振幅 a∈[0.13,0.18]（**v2.1 修订**：原下限 0.10 在冒烟中实测使约
> 27% 抽签达不到纹理可见性门——a=0.1136 时逐帧最小 |Δ|=0.0187，线性投影 a≥0.1216
> 才过线，故下限提至 0.13 留约 10% 余量；上限 0.18 不变，色彩统计门在校准乘子下
> 仍约束均值落带）；各变体倍频程集追加一个 texel 级可表示的细尺度 λ=0.08。
> **可见性门口径（v2.1 钉死）**：取**逐帧最小值** ≥ 0.02（每一帧都必须看得见纹理，
> 不允许均值口径掩盖弱帧）。
>
> **v2-4 后处理收窄。** 亮度×[0.92,1.08]、对比×[0.92,1.08]、gamma[0.90,1.12]、
> 暗角[0.10,0.35]（内窥镜实拍本有暗角，保留）、噪声 σ[0,0.008]。
>
> **v2-5 试点与验收。** seq01 × 4 种子；蒙太奇新增**真实帧参照行**（real_full/seq01）
> 供直接对照；机械验收 = 烘焙确定性、UV 覆盖率、OFF 态字节等同（沿 F 关卡）、
> F1/F2/F3 全绿、**色彩统计门**（渲染器官区均值落带内）、**纹理可见性门**（纹理渲染
> vs 同色平涂渲染的器官区 |Δ| ≥ 0.02，约 v1 实测 0.007 的 3 倍）；终门 = 所有者目检。

日期：2026-08-10
上游依据（均为已提交裁定，本文只做实例化）：`95b765f` 的
`experiments/2026-07-03_windows-render-diversity/PLAN.md` §2-C1 与
`DATA_SIDE_HANDOFF.md` §3/§4/§6-C1；2026-06-22 渲染 DR 规格（legacy 可行域）；
R25 路线结果（`9c74cf0`，程序化多尺度顶点色场，ELIGIBLE）。
前置：F1/F2/F3 三修已在本分支落地并通过 seq01 全序列字节校验。

## 1. 目标与验收

打破"灰白模"捷径：仅随机化外观（C1 单因子隔离），其余一切不动。基线对照
（2026-08-10 新鲜缓存实测）：线性可分性 1.0000、分离比 3.708、合成 RMS 1.249
（真实 7.642）、质心距 16.484。验收阶梯：
1. 机械：OFF 时字节级不变（沿 F 修的 parity 关卡）；ON 时 F1 空白守卫、F3 对账、
   `.pt` 契约全数通过；种子全记录。
2. 服务器度量：DR 变体数据集重抽特征后 `analyze_domain_gap.py` 的可分性 < 1.0000
   且合成 RMS > 1.249（方向性要求，无数值承诺）。
3. 训练级（后续独立步骤）：c2 式 synth-only 训练在真实测试上的 gap-closed %。
主张边界：可分性下降不等于迁移收益；任何 gap-closed 声明只能来自第 3 级。

## 2. 随机化域（legacy Visualizer 可行集，逐项 config 门控）

`DiversityConfig.appearance`（新增 dataclass 段，默认 enabled=False，OFF 即旧行为）：

| 旋钮 | 定义 | 范围（工程适配，冻结） |
|---|---|---|
| organ_mode | `uniform` 或 `r25_field` 二选一（按序列随机） | 概率 uniform 0.3 / r25_field 0.7 |
| uniform 色 | 肾样 albedo RGB | R∈[0.45,0.62], G∈[0.28,0.42], B∈[0.30,0.44] |
| r25_field | R25 场族逐顶点色（candidate-1/2/3 等概率），基色取 uniform 色域随机样 | 振幅 a∈[0.15,0.30] |
| background | 远离纯白的腔体色 | R∈[0.10,0.35], G∈[0.04,0.22], B∈[0.05,0.24] |
| 后处理链 | 捕获后、写盘前的 numpy 逐帧处理：亮度×[0.85,1.15]、对比×[0.85,1.15]、gamma∈[0.8,1.25]、暗角强度∈[0,0.35]、高斯噪声 σ∈[0,0.012] | 逐帧种子抖动 |
| 光照 | **不随机**（legacy 无法随机方向/强度，2026-06-22 已裁定不可行；light_on=True 原样） | — |

- R25 场求值：序列首帧顶点做 bbox-center/max-extent 归一化后求值一次，同拓扑
  逐帧复用同一组颜色字节（同一性契约，纹理不"游泳"）；模块复用 `9c74cf0` 的
  `dpost/c1_r25_procedural_microtexture.py`（2026-08-10 勘误：r25 分支合并已
  回退、并未并入本分支；模块连同其测试与 import 闭包按文件自 `9c74cf0`
  逐字节提取），不复制代码。
- 标签安全：只动像素。相机、几何、力、SampleID、配对链零接触；几何增广禁止。

## 3. 种子与溯源

每序列主种子 `seed_seq = SeedSequence([diversity.seed, seq 序号])`；spawn 出
organ/background/后处理三条子流。全部抽样值（模式、颜色、a、变体名、逐帧后处理
参数的种子）写入该序列 `replay_meta.json` 的 `appearance` 段（PLAN §4 溯源要求）。

## 4. 试点先行（本轮范围）

1. 实现 + 测试（TDD；OFF-parity 用既有 F 套件模式）。
2. 试点渲染：仅 seq01，三个不同主种子各出一版（800px 全序列 + 256px 序列化到
   scratch），产出 3×6 帧蒙太奇对比页（含基线白模一行）供人工检视。
3. **人工检视门（用户）通过前，不做 31 序列全量、不上传服务器。**
   全量与上传按 DATA_SIDE_HANDOFF §8 合格数据五关 + PLAN §4 清单执行，
   目标域名 `preprocessed/sources/synt/dr-c1-v1/`。

## 5. 排除与不做

不做 C2 视角、C3 接触点（后续单独隔离）；不做 OffscreenRenderer/PBR 迁移（仅当
C1 平坦着色的隔离增益证明不足时再议）；不做训练期光度增广（已判 LOSE）；不做
任何力生成；不改 `.pt` 契约。
