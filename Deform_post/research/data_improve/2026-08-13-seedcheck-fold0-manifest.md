# 实验配置清单 SEEDCHECK-FOLD0-20260813（折0 双臂多种子复测，C3 边际裁决战）

所有者已裁定"先执行2然后是1，今晚过夜任务完全占用这台机器"；本清单按制度交检即开跑。

## 1. 目的与预注册判定

- **目的**：C1C3-C2-20260812 的 C3 边际因折0 反号（DR-C1 0.7639 → 组合 0.7949，Δ=+0.031，在实测种子噪声带内）判"不可判定"。本实验按指定通道以多种子判定折0 反号是否种子噪声（先例 = 折3 复测）。
- **设计**：仅折0、仅 c2 协议，双臂各补 2 种子（43/44），与既有种子 42 合成每臂 n=3：DR-C1 臂（`mixed_dr_c1_v1` + splits/cv5_dr_c1）vs 组合臂（`mixed_dr_c1c3_v1` + splits/cv5_dr_c1c3）；除 `general.seed` 外协议零改动。
- **预注册判定**：组合臂 3-种子均值 < DR-C1 臂 3-种子均值 → 折0 改判"改善" → C3 边际 5/5 成立 → **判"C3 边际有效"**（最终配方含 C3）；反之折0 回退成立 → C3 边际维持"不可判定"，最终配方（DR-C1 或组合）交所有者裁定。同报各臂种子标准差。角度不入判定。

## 2. 资源与调度（今晚满机）

四卡四运行：H100=组合s43、卡1=组合s44、卡2=DR s43、卡3=DR s44。**错峰发射**（内存定律：缓存构建瞬时峰组合臂 ~236G / DR 臂 ~119G，构建不得重叠）：发射器脚本逐个启动，待前一运行确认上卡训练（GPU 利用率 ≥50% 且显存 >10G）再启动下一个；全部上卡后稳态驻留 ≈ 2×79G+2×40G=238G，预算内。预计墙钟：DR 臂 ~4–6 小时、组合臂（Ada）~10–12 小时，明晨出齐。输出 `outputs/seedcheck_fold0/<臂_种子>/`；停止条件沿 DR-C2 清单 §5。

## 3. 术语表

沿用 SEEDCHECK-FOLD3-20260811 清单术语表（种子噪声 / seed-mean 判定 / 臂）；新增：**错峰发射** = 按内存定律将各运行的缓存构建期严格串行错开的启动方式（来路：C1C3 清单 §2c 内存定律）。

## 4. 运行回执（2026-08-13 晨填写）

- 执行变更申报：因同事（hongtao，容器 ecgegg 的 vLLM 推理服务）占用三块 Ada，经所有者裁定改为 **H100 单卡串行**（"H100单卡执行，这个推理服务是同事的工作，不能关停"）；四运行 17:27→03:49 全部 rc=0，同事服务零接触，昨夜本机会话中断未影响服务器侧执行。
- **折0 magMAE 实测**：

| 臂 | 种子42 | 种子43 | 种子44 | **seed-mean** | 种子标准差 |
|---|---|---|---|---|---|
| DR-C1（mixed_dr_c1_v1） | 0.7639 | 0.8806 | 0.8107 | **0.8184** | 0.0587 |
| 组合（mixed_dr_c1c3_v1） | 0.7949 | 0.9331 | 0.7989 | **0.8423** | 0.0787 |

- **预注册判定：组合均值 0.8423 ≥ DR-C1 均值 0.8184 → 折0 回退成立（seed-mean 口径 Δ=+0.024）→ C3 边际维持"不可判定"**；最终配方（DR-C1 或组合）交所有者裁定。
- 诚实语境（不改变上述判定）：折0 的 seed-mean 差 +0.024 仍小于两臂种子标准差（0.059/0.079）——折0 上 C3 至多"无益或轻微有害"；而折1–4 的配对改善为 −0.055/−0.208/−0.157/−0.791，pooled 边际 −0.236（0.9114→0.6755）远超噪声。即：**C3 的总体收益证据充分，"5/5 干净同号"的严格标签未达成**。

