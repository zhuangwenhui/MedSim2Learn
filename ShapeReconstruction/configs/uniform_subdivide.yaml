# Uniform subdivision: splits every face, increasing resolution uniformly.
mode: uniform_subdivide

uniform_subdivide:
  iterations: 1

# Post-processing
voxel_spacing_mm: 1.0
restore_physical_coords: true
mesh_quality_fix: true
canonicalize_pose: false
